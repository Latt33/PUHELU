[project]
name = "backend"
version = "0.1.0"
description = "COPD Risk Analysis API"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "pydantic",
    "sqlalchemy",
    "uvicorn",
    "numpy",
    "matplotlib",
    "librosa",
    "soundfile",
    "python-multipart",
]
